public class NewClass {
}



    private int solve(arr, N, sum)
    int[][] boolean = new boolean[N+1][sum+1];

            for(int r = 0; r < N; r++){
        dp[r][0] = true
        }

        for(int c = 0; c < sum + 1; c++){
        dp[0][c] = false;
        }

        dp[0][0] = true;

        for(int i = 1; i < N; i++){
        for(int j = )
        }

        }